# 🔒 فصل البيانات - MAP Supplies

## ✅ تم فصل البيانات بنجاح!

الآن أصبحت بيانات الإدارة **منفصلة تماماً** عن بيانات العملاء.

---

## 📊 مفاتيح التخزين

### بيانات العملاء (index.html + script.js):

| المفتاح | الوصف |
|---------|-------|
| `customerSession` | جلسة العميل الحالي |
| `customer_cart` | سلة التسوق الخاصة بالعميل |

**ملاحظة**: العميل يقرأ فقط من بيانات الإدارة ولا يكتب فيها:
- يقرأ من `admin_orders` لرؤية طلباته
- يقرأ من `admin_quotes` لرؤية عروض الأسعار
- يكتب طلبات جديدة في `admin_orders`

---

### بيانات الإدارة (admin.html + admin-script.js):

| المفتاح | الوصف |
|---------|-------|
| `adminSession` | جلسة المدير/المشرف الحالي |
| `admin_orders` | جميع الطلبات |
| `admin_customers` | قائمة العملاء |
| `admin_supervisors` | قائمة المشرفين الفرعيين |
| `admin_quotes` | عروض الأسعار |
| `admin_notifications` | سجل الإشعارات |
| `admin_notificationTemplate` | قالب رسائل الإشعارات |

---

## 🔐 الأمان والخصوصية

### ما الذي تم فصله:

✅ **الجلسات منفصلة تماماً**
- جلسة العميل: `customerSession`
- جلسة الإدارة: `adminSession`
- لا يمكن لأي منهما الوصول لجلسة الآخر

✅ **السلة خاصة بالعميل فقط**
- `customer_cart` - لا يمكن للإدارة رؤيتها

✅ **بيانات الإدارة محمية**
- جميع البيانات الإدارية تبدأ بـ `admin_`
- لا يمكن للعميل التعديل عليها (قراءة فقط)

✅ **صفحات منفصلة**
- `index.html` - للعملاء فقط
- `admin.html` - للإدارة فقط (محمية برمز `map2026`)

---

## 🔄 كيف تعمل البيانات المشتركة؟

### الطلبات (`admin_orders`):

1. **العميل يكتب**:
   - عند إرسال طلب جديد، يُضاف إلى `admin_orders`

2. **الإدارة تقرأ وتعدل**:
   - تقرأ جميع الطلبات
   - تضيف عروض أسعار
   - تحدث حالة الطلبات

3. **العميل يقرأ**:
   - يقرأ طلباته فقط (حسب `customerId`)
   - يرى عروض الأسعار الخاصة بطلباته

### عروض الأسعار (`admin_quotes`):

1. **الإدارة تكتب**:
   - تضيف عروض أسعار جديدة

2. **العميل يقرأ ويتفاعل**:
   - يرى عروض الأسعار لطلباته
   - يقبل أو يرفض العروض

---

## 🛡️ الحماية

### ما لا يمكن للعميل فعله:

❌ رؤية طلبات العملاء الآخرين
❌ رؤية بيانات العملاء الآخرين
❌ الوصول لقائمة المشرفين
❌ رؤية سجل الإشعارات
❌ تعديل حالة الطلبات مباشرة
❌ إضافة عروض أسعار

### ما لا يمكن للإدارة فعله:

❌ رؤية سلة تسوق العملاء قبل إرسال الطلب
❌ الوصول لجلسات العملاء

---

## 📝 مثال عملي

### سيناريو: عميل يرسل طلب

1. **العميل (index.html)**:
   ```javascript
   // يضيف منتجات للسلة (customer_cart)
   cart.push(item);
   
   // يرسل الطلب
   const order = { customerId: currentUser.id, items: cart };
   localStorage.setItem('admin_orders', ...); // يكتب في بيانات الإدارة
   ```

2. **الإدارة (admin.html)**:
   ```javascript
   // تقرأ الطلبات
   orders = localStorage.getItem('admin_orders');
   
   // تضيف عرض سعر
   quotes.push({ orderId, price, ... });
   localStorage.setItem('admin_quotes', ...);
   ```

3. **العميل (index.html)**:
   ```javascript
   // يقرأ عروض الأسعار
   const quotes = localStorage.getItem('admin_quotes');
   const myQuotes = quotes.filter(q => q.orderId === myOrderId);
   
   // يقبل عرض السعر
   quote.status = 'accepted';
   localStorage.setItem('admin_quotes', ...);
   ```

---

## 🧹 مسح البيانات

### لمسح بيانات العملاء فقط:
```javascript
localStorage.removeItem('customerSession');
localStorage.removeItem('customer_cart');
```

### لمسح بيانات الإدارة فقط:
```javascript
localStorage.removeItem('adminSession');
localStorage.removeItem('admin_orders');
localStorage.removeItem('admin_customers');
localStorage.removeItem('admin_supervisors');
localStorage.removeItem('admin_quotes');
localStorage.removeItem('admin_notifications');
localStorage.removeItem('admin_notificationTemplate');
```

### لمسح كل شيء:
```javascript
localStorage.clear();
```

---

## ✨ الفوائد

1. **أمان أفضل**: لا يمكن للعملاء الوصول لبيانات حساسة
2. **خصوصية**: كل عميل يرى طلباته فقط
3. **تنظيم**: البيانات منظمة ومفصولة بوضوح
4. **صيانة أسهل**: يمكن تطوير كل جزء بشكل مستقل
5. **أداء أفضل**: كل صفحة تحمل فقط ما تحتاجه

---

## 🔍 التحقق من الفصل

لتتأكد أن البيانات منفصلة:

1. افتح `index.html` وسجل كعميل
2. افتح Console (F12)
3. اكتب: `localStorage`
4. ستجد:
   - `customerSession` - جلسة العميل
   - `customer_cart` - سلة العميل
   - `admin_orders` - الطلبات (مشتركة)
   - `admin_quotes` - العروض (مشتركة)

5. افتح `admin.html` وسجل كمدير
6. افتح Console (F12)
7. اكتب: `localStorage`
8. ستجد:
   - `adminSession` - جلسة الإدارة
   - `admin_*` - جميع بيانات الإدارة

---

**الآن البيانات منفصلة وآمنة! 🎉**
